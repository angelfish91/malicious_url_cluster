#include <stdio.h>
#include <string.h>
#include <string>
#include <vector>
#include <assert.h>


std::vector<std::string> max_substring_of_strings(std::vector<std::string> raw_strings, int min_char_count);